# Durus — Task Breakdown

Source of truth: `plans/2026-09-13_plan.md`. Tasks marked 🔒 require explicit
user approval before running (scaffold/install/cloud).

> **Build status (2026-09-13):** Phases 1–3 + fees ledger (Phase 4) built,
> backend migrations 001–006 applied live, notifications shipped (was Phase 8),
> parent portal v1 (Phase 6 core) built. `flutter analyze` 0 issues, 13/13
> tests green, `flutter build web` + `flutter build apk --debug` succeed.
>
> **Fix loop after first real-run smoke test:** (1) notifications now refresh
> live via Postgres realtime (`supabase_realtime` on `notifications` +
> channel in HomeShell); (2) fee month field validates `YYYY-MM` + select-all
> on tap; (3) signup shows real server errors + "check your email" state when
> email confirmation is enabled.
>
> **Feature loop (weekly calendar, attendance history, monthly reports):**
> Added migration 006 (`generate_report` RPC + unique constraint on reports),
> weekly calendar tab (جدول الأسبوع with prev/this/next week navigation),
> attendance history section on student detail, and monthly report screen
> (month picker, attendance/fee/tests/notes sections, idempotent RPC).
>
> **Loop (teacher status + announcements compose + portal pass):** migration
> 007 adds `profiles.active` + `set_teacher_active` RPC; manager sees a status
> chip + toggle per teacher and deactivated accounts hit an app-level gate.
> Migration 008/009 fixed `create_teacher_with_credentials` (it called the
> non-existent `auth.admin_create_user`; now direct GoTrue-complete inserts).
> Announcements can be composed from home. Parent portal verified overflow-free
> at 390px and 320px. `flutter analyze` 0 issues, 14/14 tests green, web build
> succeeds; smoke-tested live (compose, disable → blocked login, re-enable).

---

## Phase 1 — Foundations

- [x] 🔒 git init in `durus/` + .gitignore (Flutter + Supabase + env)
- [x] 🔒 `flutter create` in `src/` (org: com.amworx.durus, platforms: android, web)
- [x] 🔒 Create Supabase project (free tier) + link credentials to project config
- [x] Write `supabase/` migration 001: schema per plan §4 (all tables + RLS)
- [x] Auth: teacher/manager email+password via Supabase Auth
- [x] App shell: Arabic-only, RTL, go_router, Riverpod bootstrap, theme
- [x] First-launch wizard: single vs multi → set `is_manager` / role
- [x] Login + session handling (teacher/manager)
- [x] `flutter analyze` + `flutter test` green (base test suite)

## Phase 2 — Teacher core

- [x] Students CRUD (profile, grade, subjects, default location, parent)
- [x] Subjects CRUD (name, grade, notes)
- [x] Recurring weekly slots (student, subject, day, time, location)
- [x] Home dashboard wiring (today's sessions, counts)

## Phase 3 — Attendance

- [x] Weekly calendar view
- [x] One-tap attendance: present / absent / rescheduled
- [x] Session notes
- [x] Attendance history per student

## Phase 4 — Fees

- [x] Variable monthly fee ledger per student (amount, due date)
- [x] Payment recording (partial/paid, date, method, note)
- [x] Overdue list ("who owes") + month summary

## Phase 5 — Progress

- [x] Tests/results CRUD (monthly/midterm/final, score)
- [x] Free-form notes
- [x] Monthly report generation (attendance %, fees status, latest scores, notes)

## Phase 6 — Parents portal

- [x] PIN generation per student (hashed token) + link
- [x] Parent portal screens (responsive web): upcoming, attendance, fees,
      notes/tests/reports
- [x] Simple UX pass (non-technical parents; any device) — verified
      overflow-free at 390px and 320px; entry + home render correctly

## Phase 7 — Multi-teacher + admin

- [x] Invitation code/link flow (manager → teacher)
- [x] Manager creates teacher account + share credentials (fixed: migration 008/009)
- [x] Disable/remove teacher (active flag + app gate; remove = future)
- [ ] Admin dashboard screens (all students/teachers/fees/reports/settings)
- [x] Announcements (compose from home + display; teachers get notified)

## Phase 8 — Polish

- [x] Notifications (in-app centers shipped; decide WhatsApp share deep-link;
      FCM push reserved via `notifications.pushed_at`)
- [ ] Backups (scheduled)
- [ ] Supabase Storage for photos (homework/exams)
- [ ] Release build (Android APK/AAB)

---

## Remaining (next loop)

- Admin dashboard screens (all students/teachers/fees/reports/settings) — Phase 7
- "Remove teacher" (hard delete) — currently only disable/enable exists
- Server-side enforcement of disabled accounts (RLS/auth hook; app gate only in v1)
- Backups, storage, release build (Phase 8 remainder)
- First real-device smoke test (web portal + Android app against live DB)