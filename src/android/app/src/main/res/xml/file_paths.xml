<?xml version="1.0" encoding="utf-8"?>
<!-- FileProvider paths: exposes only the in-app updater's cache subfolder so
     the system package installer can read the downloaded APK. -->
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <cache-path
        name="updates"
        path="updates/" />
</paths>